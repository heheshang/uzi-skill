# Rust / Cargo 构建产物
target/
**/*.rs.bk
*.pdb

# 分析缓存与报告（每次运行重新生成，用户私有）
.cache/
**/.cache/
reports/
**/.reports/
**/reports/

# 例外：uzi-screen 的黄金夹具是一份**签入**的 ticker 缓存
# （`--versus` / `--portfolio` 的对比基线）。目录名沿用上游的 `.cache` 布局，
# 但它不是运行产物 —— 必须随仓库分发，否则干净 clone 跑测试会挂。
# 注意：negation 必须排在排除规则之后，且要显式放行目录本身（git 只在父目录
# 未被排除时才会向下遍历）。
!crates/uzi-screen/tests/fixtures/.cache/
!crates/uzi-screen/tests/fixtures/.cache/**

# Python 工具脚本（tools/ 下的对照 / golden 脚本）产生的缓存
__pycache__/
*.py[cod]

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env（存放 MX_APIKEY 等敏感凭据，绝不 commit）
.env
.env.local
.env.*.local
*.local.env
*.key

# 防御性 ignore：cookies / session / 私人状态文件
**/cookies.json
**/.uzi-skill/
*.pem
*.p12
id_rsa*
.netrc

# 本地草稿（不进远端）
docs/*.local.md

# release 附件的整页截图
docs/screenshots/full-page.png
